# Frontend Dashboard

## Setup

```bash
cp .env.example .env
npm install
npm run dev
```

App runs on `http://localhost:3000`.

Configure API base URL in `.env`:

```bash
VITE_API_BASE_URL=http://YOUR_SERVER:8000
```
